--- a/projects/amdsmi/include/amd_smi/impl/amdgpu_drm.h
+++ b/projects/amdsmi/include/amd_smi/impl/amdgpu_drm.h
@@ -1627,6 +1627,14 @@
 #define AMDGPU_FAMILY_GC_12_0_0                        152 /* GC 12.0.0 */

 /* FIXME wrong namespace! */
+/* Only define struct drm_color_ctm_3x4 if it hasn't been defined by libdrm.
+ * Newer versions of libdrm (2.4.131+) define this struct in drm_mode.h.
+ * Since this file includes libdrm/drm.h above, the struct may already exist.
+ * We use DRM_MODE_OBJECT_ANY as a proxy - if libdrm headers are included,
+ * this macro will be defined, and we can skip our local definition.
+ */
+#if !defined(DRM_MODE_OBJECT_ANY)
+/* libdrm not available, define the struct ourselves */
 struct drm_color_ctm_3x4 {
        /*
         * Conversion matrix with 3x4 dimensions in S31.32 sign-magnitude
@@ -1634,6 +1642,7 @@
         */
        __u64 matrix[12];
 };
+#endif

 #if defined(__cplusplus)
 }
